import React from 'react'

import { PageTemplate, Header, Footer } from 'components'

const CompanyPage = () => (
  <PageTemplate header={<Header />} footer={<Footer />}>
    <p>company page (TODO: add company container)</p>
  </PageTemplate>
)

export default CompanyPage
